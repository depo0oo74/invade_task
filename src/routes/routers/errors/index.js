// ** import pages
import NotFound from "../../../pages/errors/NotFound.jsx";

// ** errors routes
const errorsRoutes = [
    {
        path: "/",
        component: NotFound,
        layout: "default"
    }
];

export default errorsRoutes;